<template name="title-img">
  <view class="little-one" data-sourceid="{{item.sourceid}}" data-platid="{{item.platid}}" data-ctype="{{item.ctype}}" bindtap='itemClick'>

    <view class="little-one-text">
      <view class="stream-item-title" wx:if="{{item.title}}">{{item.title}}</view>
      <view class="stream-item-info">{{item.author}} {{item.showTime}}</view>
    </view>
    <view class="little-one-photo">
      <image src="{{item.imgs[0].imgurl}}" mode='widthFix'></image>
    </view>
  </view>
</template>